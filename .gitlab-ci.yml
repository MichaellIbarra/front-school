# GitLab CI/CD Pipeline para EduAssist Frontend
# Este pipeline construye la aplicación React y publica la imagen Docker en Docker Hub

# Variables globales
variables:
  # Nombre de la imagen Docker
  DOCKER_IMAGE_NAME: "vg-web-school"
  # Registro de Docker Hub (puede ser tu username o tu organización)
  DOCKER_REGISTRY: "docker.io"
  # Versión de Node.js para el build
  NODE_VERSION: "20"
  # Configuraciones de Docker
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  DOCKER_BUILDKIT: 1

# Etapas del pipeline
stages:
  - test
  - build
  - deploy

# Cache para optimizar builds
cache:
  paths:
    - node_modules/
    - .npm/

# Etapa de pruebas y linting
test:
  stage: test
  image: node:${NODE_VERSION}-alpine
  before_script:
    - npm ci --cache .npm --prefer-offline --legacy-peer-deps
  script:
    - echo "🧪 Ejecutando pruebas..."
    - npm run lint || echo "⚠️ Linting failed, but continuing..."
    - npm test -- --coverage --ci --silent --passWithNoTests --watchAll=false
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage/cobertura-coverage.xml
    expire_in: 1 week
  only:
    - branches
  except:
    - tags

# Etapa de construcción y publicación Docker
build_and_push:
  stage: build
  image: docker:24-dind
  services:
    - docker:24-dind
  variables:
    # Configuración para Docker-in-Docker
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: "/certs"
    DOCKER_TLS_VERIFY: 1
    DOCKER_CERT_PATH: "$DOCKER_TLS_CERTDIR/client"
    DOCKER_BUILDKIT: 1
  before_script:
    # Verificar que las variables estén configuradas
    - |
      if [ -z "$DOCKER_HUB_USERNAME" ]; then
        echo "❌ ERROR: DOCKER_HUB_USERNAME no está configurado"
        exit 1
      fi
    - |
      if [ -z "$DOCKER_HUB_TOKEN" ]; then
        echo "❌ ERROR: DOCKER_HUB_TOKEN no está configurado"
        exit 1
      fi
    - echo "$DOCKER_HUB_TOKEN" | docker login $DOCKER_REGISTRY -u "$DOCKER_HUB_USERNAME" --password-stdin
  script:
    - echo "📦 Construyendo imagen Docker con BuildKit..."
    - |
      # Generar tags basados en la rama y commit
      if [ "$CI_COMMIT_REF_NAME" = "main" ] || [ "$CI_COMMIT_REF_NAME" = "master" ]; then
        TAGS="latest $CI_COMMIT_SHA"
      else
        TAGS="$CI_COMMIT_REF_NAME $CI_COMMIT_SHA"
      fi
    
    # Construir la imagen con BuildKit para mejor performance
    - |
      for tag in $TAGS; do
        full_tag="$DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:$tag"
        echo "�️ Construyendo y publicando $full_tag"
        docker buildx build --push --platform linux/amd64 -t $full_tag .
      done
    
    - echo "✅ Imagen publicada exitosamente en Docker Hub"
  after_script:
    - docker logout $DOCKER_REGISTRY || true
  timeout: 30m
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

# Etapa de deployment (opcional - para tags de release)
deploy_production:
  stage: deploy
  image: alpine:latest
  script:
    - echo "🚀 Desplegando versión $CI_COMMIT_TAG a producción..."
    - echo "Para ejecutar la imagen:"
    - echo "docker run -d -p 3000:80 --name vg-web-school $DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:$CI_COMMIT_TAG"
  only:
    - tags
  when: manual

# Despliegue automático para develop
deploy_develop:
  stage: deploy
  image: alpine:latest
  script:
    - echo "🚀 Desplegando rama develop..."
    - echo "Comando para desplegar:"
    - echo "docker pull $DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:develop"
    - echo "docker stop eduassist-develop || true"
    - echo "docker rm eduassist-develop || true"
    - echo "docker run -d -p 3001:80 --name eduassist-develop --restart unless-stopped $DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:develop"
  only:
    - develop
  when: on_success

# Despliegue automático para main/master (producción)
deploy_main:
  stage: deploy
  image: alpine:latest
  script:
    - echo "🚀 Desplegando a producción..."
    - echo "Comando para desplegar:"
    - echo "docker pull $DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:latest"
    - echo "docker stop vg-web-school || true"
    - echo "docker rm vg-web-school || true"
    - echo "docker run -d -p 3000:80 --name vg-web-school --restart unless-stopped $DOCKER_HUB_USERNAME/$DOCKER_IMAGE_NAME:latest"
  only:
    - main
    - master
  when: on_success

# Job para limpiar imágenes locales (opcional)
cleanup:
  stage: deploy
  image: docker:24-dind
  services:
    - docker:24-dind
  script:
    - docker system prune -f
  when: manual
  allow_failure: true